---
---

<section class="reviews-v2">
  <div class="container">
    <!-- IMPORTANT: reuse the SAME surface class used by the STICKY section -->
    <div class="sticky-surface reviews-v2__surface">
      <header class="reviews-v2__header">
        <h2 class="reviews-v2__title">Why companies ❤ GameLayer</h2>
        <p class="reviews-v2__lead">
          GameLayer powers engagement mechanics for products used by millions of users. These figures reflect aggregated usage across customer programmes and live environments.
        </p>
      </header>

      <!-- 4-panel narrative (Step-Up style) -->
      <div class="reviews-v2__panels" aria-label="Why companies love GameLayer (narrative)">
        <div class="reviews-v2__panel reviews-v2__panel--1">
          <div class="reviews-v2__num">1</div>
          <div class="reviews-v2__panelTitle">Sustainable engagement</div>
          <div class="reviews-v2__panelText">
            Design for repeat participation, not one-off campaigns. Use missions, streaks, and rewards to keep users coming back over time — without relying on constant promotions.
          </div>
        </div>

        <div class="reviews-v2__panel reviews-v2__panel--2">
          <div class="reviews-v2__num">2</div>
          <div class="reviews-v2__panelTitle">Progress users can feel</div>
          <div class="reviews-v2__panelText">
            Make improvement visible through XP, levels, and achievements. Give users clear goals, momentum, and a sense of advancement that builds motivation naturally.
          </div>
        </div>

        <div class="reviews-v2__panel reviews-v2__panel--3">
          <div class="reviews-v2__num">3</div>
          <div class="reviews-v2__panelTitle">Social motivation</div>
          <div class="reviews-v2__panelText">
            Encourage friendly competition and collaboration with teams and leaderboards. Turn individual actions into shared experiences - at scale - that users want to participate in together.
          </div>
        </div>

        <div class="reviews-v2__panel reviews-v2__panel--4">
          <div class="reviews-v2__num">4</div>
          <div class="reviews-v2__panelTitle">Built for real programmes</div>
          <div class="reviews-v2__panelText">
            Run production-ready engagement with rules, eligibility, budgets, and rewards. Control how mechanics behave and scale programmes with confidence.
          </div>
        </div>
      </div>

      <!-- Metrics (must remain unchanged) -->
      <div class="reviews-v2__metrics" aria-label="Aggregated proof metrics">
        <div class="reviews-v2__metric">
          <div class="reviews-v2__metricValue">75%</div>
          <div class="reviews-v2__metricLabel">of users engaged weekly</div>
        </div>
        <div class="reviews-v2__metric">
          <div class="reviews-v2__metricValue">60%</div>
          <div class="reviews-v2__metricLabel">boost in brand sentiment</div>
        </div>
        <div class="reviews-v2__metric">
          <div class="reviews-v2__metricValue">25%</div>
          <div class="reviews-v2__metricLabel">increase in session time</div>
        </div>
        <div class="reviews-v2__metric">
          <div class="reviews-v2__metricValue">200%</div>
          <div class="reviews-v2__metricLabel">increase in task completion</div>
        </div>
        <div class="reviews-v2__metric">
          <div class="reviews-v2__metricValue">50%</div>
          <div class="reviews-v2__metricLabel">boost in product awareness</div>
        </div>
        <div class="reviews-v2__metric">
          <div class="reviews-v2__metricValue">40%</div>
          <div class="reviews-v2__metricLabel">increase in no. of sessions</div>
        </div>
      </div>

      <div class="reviews-v2__footnote">Metrics are indicative and vary by implementation and use case</div>
    </div>
  </div>
</section>

<style>
  /* Scoped styles only */
  .reviews-v2 {
    padding: clamp(56px, 6vw, 96px) 0;
  }

  .reviews-v2__surface {
    /* no width rules here — sticky-surface controls width + padding */
  }

  .reviews-v2__header {
    text-align: center;
    margin-bottom: clamp(28px, 3vw, 40px);
  }

  .reviews-v2__title {
    font-size: clamp(34px, 4vw, 56px);
    line-height: 1.05;
    margin: 0 0 14px 0;
    font-weight: 800;
  }

  .reviews-v2__lead {
    margin: 0 auto;
    max-width: 920px;
    font-size: 18px;
    line-height: 1.6;
    opacity: 0.85;
  }

  /* Panels */
  .reviews-v2__panels {
    display: grid;
    grid-template-columns: repeat(4, minmax(0, 1fr));
    border-radius: 28px;
    overflow: hidden;
    margin: clamp(28px, 3vw, 40px) 0 clamp(32px, 4vw, 48px);
  }

  .reviews-v2__panel {
    padding: 28px;
  }

  .reviews-v2__panel--1 { background: #EAF4FF; }
  .reviews-v2__panel--2 { background: #ECF8F1; }
  .reviews-v2__panel--3 { background: #FFF8D6; }
  .reviews-v2__panel--4 { background: #FCEEF4; }

  .reviews-v2__num {
    font-size: 44px;
    font-weight: 900;
    line-height: 1;
    margin-bottom: 12px;
  }

  .reviews-v2__panelTitle {
    font-size: 18px;
    font-weight: 800;
    margin-bottom: 10px;
    line-height: 1.2;
  }

  .reviews-v2__panelText {
    font-size: 16px;
    line-height: 1.6;
    opacity: 0.85;
  }

  /* Metrics: 6 items in ONE ROW on desktop */
  .reviews-v2__metrics {
    display: grid;
    grid-template-columns: repeat(6, minmax(0, 1fr));
    gap: clamp(18px, 2vw, 28px);
    align-items: start;
    text-align: center;
    margin-top: 6px;
  }

  .reviews-v2__metricValue {
    font-size: clamp(42px, 4vw, 64px);
    font-weight: 900;
    line-height: 1;
    margin-bottom: 8px;
  }

  .reviews-v2__metricLabel {
    font-size: 15px;
    line-height: 1.3;
    opacity: 0.85;
  }

  .reviews-v2__footnote {
    text-align: center;
    margin-top: 24px;
    font-size: 15px;
    opacity: 0.7;
  }

  /* Responsive */
  @media (max-width: 1024px) {
    .reviews-v2__panels { grid-template-columns: repeat(2, minmax(0, 1fr)); }
    .reviews-v2__metrics { grid-template-columns: repeat(3, minmax(0, 1fr)); }
  }

  @media (max-width: 640px) {
    .reviews-v2__panels { grid-template-columns: 1fr; border-radius: 24px; }
    .reviews-v2__metrics { grid-template-columns: repeat(2, minmax(0, 1fr)); }
  }
</style>
